prototype(MyVendor.AwesomeNeosProject:Component.AuthorInfo) < prototype(Neos.Neos:ContentComponent) {
    label = ''
    renderer = afx`
        <div @if.is={props.label}>{props.label}</div>
        `

    @process.debug = Neos.Fusion:Debug.Console {
        value = ${node}
    }
}
