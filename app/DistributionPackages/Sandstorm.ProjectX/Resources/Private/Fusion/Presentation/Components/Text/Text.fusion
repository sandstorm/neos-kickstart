prototype(Sandstorm.ProjectX:Component.Text) < prototype(Neos.Fusion:Component) {
    text = ''

    @propTypes {
        # validation rules for props
        text = ${PropTypes.string}
    }

    renderer = afx`
        <div>{props.text}</div>
    `
}
